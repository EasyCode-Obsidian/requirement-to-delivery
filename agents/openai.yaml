interface:
  display_name: "Requirement to Delivery"
  short_description: "Turn requirements into tracked execution — from quick fixes to multi-phase projects"
  default_prompt: "Use $requirement-to-delivery to handle this request. Automatically classify it as quick-fix, single-phase, or multi-phase, then follow the appropriate execution depth: direct fix for small tasks, full planning and todo tracking for larger work."
